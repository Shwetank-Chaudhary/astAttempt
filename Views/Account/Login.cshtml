@model astAttempt.Models.Entity.Employee
@{
    ViewData["Title"] = "Login as Admin";
}
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login and Registration</title>
    <link rel="stylesheet" href="/css/Register.css" asp-append-version="true" />
</head>

<body>
    <div class="container">
        <h1>Welcome</h1>
        <div class="tabs">
            <div class="tab active" onclick="showForm('login')"><h2>Login</h2></div>
            @* <div class="tab" onclick="showForm('register')">Register</div> *@
        </div>
        <form class="form active" asp-controller="account" asp-action="login" method="post">
            <div class="form-group">
                <label label asp-for="EmpEmail">Email:</label>
                <input type="text" id="loginEmail" asp-for="EmpEmail" required>
            </div>
            <div class="form-group">
                <label asp-for="Password">Password:</label>
                <input type="password" asp-for="Password" required>
            </div>
            <button type="submit">Login</button>
        </form>


    </div>
</body>
</html>
